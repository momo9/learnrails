<p>
<%= link_to 'new', new_article_path %>
</p>

<table>
    <tr>
        <th>Title</th>
        <th></th>
    </tr>
    <% @articles.each do |item| %>
        <tr>
            <td>
                <%= link_to item.title, article_path(item) %>
            </td>
            <td>
                <%= link_to 'delete', article_path(item), method: :delete %>
            </td>
        </tr>
    <% end %>
</table>
